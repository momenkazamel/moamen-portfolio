// Deprecated: the standalone "Creative Stack" section was folded into the
// footer's "Tools of the craft." block (see components/layout/footer.tsx).
// Nothing imports this file anymore. It's kept in place rather than
// deleted only because this sandbox can't remove files.
export {};
